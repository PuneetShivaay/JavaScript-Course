var num = prompt(parseInt("Enter a Number : "));

if (num >= 50 && num <= 100) {
    document.write("Number " + num + " is within range of 50-100 ");
} else {
    document.write("Number " + num + " is not within range of 50-100 ");
}
